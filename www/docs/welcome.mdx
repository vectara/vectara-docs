---
id: introduction
slug: /
title: The Vectara Platform
sidebar_label: The Vectara Platform
---

import { Grid } from "@site/src/components/ui/Grid";
import { Spacer } from "@site/src/components/ui/Spacer";
import { TopicButton } from "@site/src/components/ui/TopicButton";
import CodePanel from '@site/src/theme/CodePanel';

<Grid columns={2}>
  <TopicButton
    title="Release Notes"
    href="/docs/release-notes"
  >
    Discover new product features, major updates, and key improvements to the documentation.
  </TopicButton>
  <TopicButton title="Changelog" href="/docs/changelog">
    Track all documentation updates, improvement, API enhancements, and bug fixes.
  </TopicButton>
</Grid>

<Spacer size="l" />

<Grid columns={2}>
  <TopicButton
    title="API Reference V2"
    href="/docs/rest-api/vectara-rest-api-v-2"
  >
    Try out Vectara's REST APIs directly in your browser.
  </TopicButton>
  <TopicButton title="Quick Start tutorial" href="/docs/quickstart">
    Use Vectara to ground answers in your data in a few minutes.
  </TopicButton>
</Grid>

<Spacer size="l" />

<p>
  <Config v="names.product" /> is an end-to-end Retrieval Augmented Generation
  as-a-Service (RAGaaS) platform for product builders to embed powerful
  generative AI capabilities into applications with extraordinary results.{" "}
  <Config v="names.product" /> offers significant improvements over traditional
  searches by understanding the context and meaning of your data. This
  revolutionary technology enables <Config v="names.product" /> to drive
  insights and provide more accurate responses to user queries, assisting
  decision-making processes. User data remains secure because{" "}
  <Config v="names.product" /> never trains on customer data.
</p>

## Welcome to the Answer Engine

The <Config v="names.product"/> team envisions a future where generative AI powers every
application to deliver contextually accurate responses and give the right
answers and actions. <Config v="names.product"/> is built on a solid **hybrid search** core
to enable better generative outcomes. Traditional search technologies focus
on keywords, which limit their ability to understand complex queries. <Config v="names.product"/>
deploys advanced **zero-shot** models and **conversational search**
capabilities to understand, interpret, and respond to user queries with remarkable
precision.

Vectara summarizes search results on complex queries while providing factual
citations from your data. <Config v="names.product"/> provides the best hybrid search
core and superior language understanding for ingestion and retrieval. <Config v="names.product"/>
can become your answer engine.

## Developer-focused, API-first, Secure

Designed for developers with an API-first approach, <Config v="names.product"/> is
the optimal choice to **integrate generative AI search** into your
applications. This complete end-to-end platform provides easy ingestion and
simple APIs. The <Config v="names.product"/> Generative AI platform enables
developers with the flexibility to build a [wide range of applications](/docs/use-case-exploration) with
powerful search experiences.

The <Config v="names.product"/> platform never trains on customer data which enables
businesses to embed generative AI capabilities without the risk of data or
privacy violations. <Config v="names.product"/> provides support for customer-managed
keys, encryption at rest and during transit, client-configurable data
retention, and more.

### :star2: Ready to Dive In? Check Out Our API Reference! :star2:

If you're ready to dive into our APIs, make your way to our [**API Reference V2.0**](/docs/rest-api/vectara-rest-api-v-2)!
This interactive environment allows you to experiment with <Config v="names.product"/>'s REST APIs
directly from your browser! Tailored for developers, the API Reference
offers a hands-on experience to understand and demonstrate our capabilities.

## Solve the Hallucination Problem

AI content generators often create **hallucinations** – false information
outside of the raw, factual data - they make stuff up. These hallucinations
lead to inaccurate and misleading responses <Config v="names.product"/> addresses
this problem through **Retrieval Augmented Generation (RAG)**, meaning it grounds the search
results in the uploaded data. By focusing on facts and reducing
hallucinations, <Config v="names.product"/> enhances trust in AI-powered decision making.

## Language Agnostic

Use <Config v="names.product"/> to search across multiple languages, eliminating language
barriers and enabling users to find what they need, regardless of the
language they use. This **cross-language** approach provides a seamless
search experience for users around the world. The best answer may be
written in German but a user asked the question in Spanish.

The <Config v="names.product"/> platform is more than just an AI product. It is
a pioneer in the realm of **neural search**, leading the way to harness the
power of your data. <Config v="names.product"/> wants to transform the way
developers interact with data and unlock a world of insights at their
fingertips. Welcome to the future of information interaction!

## Not sure where to start?

If you're new to <Config v="names.product"/>, sign up for an account [here](https://console.vectara.com/signup) and 
start your [30-day free trial](/docs/vectara-trial). Here are some other ideas:

- Check out our [Quick Start Tutorial](/docs/quickstart) and [API Recipes](/docs/api-recipes) to
  index and search your first document in just a couple of minutes!
- Check out the [Vectara Python SDK](/docs/sdk/vectara-python-sdk) with essential workflows and 
practical code examples.
- View some [Vectara applications, demos, and video tutorials](https://vectara.com/demos/).
- View our [Release Notes](/docs/release-notes) and learn about the latest features and product documentation updates.
- Visit our [forums](https://discuss.vectara.com/) to ask any questions or provide any suggestions.


// Basic collapsible with line highlighting
  <CodePanel
    snippets={[{
      language: 'python',
      code: `import requests
  import json

  def fetch_data(api_key, endpoint):
      """Fetch data from the API endpoint"""
      headers = {
          'Authorization': f'Bearer {api_key}',
          'Content-Type': 'application/json'
      }
      
      try:
          response = requests.get(endpoint, headers=headers)
          response.raise_for_status()
          return response.json()
      except requests.exceptions.RequestException as e:
          print(f"Error fetching data: {e}")
          return None

  # Example usage
  api_key = "your-api-key-here"
  endpoint = "https://api.example.com/data"
  data = fetch_data(api_key, endpoint)

  if data:
      print(json.dumps(data, indent=2))
  else:
      print("Failed to fetch data")`
    }]}
    highlightLines="5-10,15"
  />

  // Multiple language tabs
  <CodePanel
    tabs={true}
    snippets={[
      {
        language: 'curl', 
        code: `curl -X POST https://api.vectara.io/v2/corpora \\
    -H "Authorization: Bearer YOUR_API_KEY" \\
    -H "Content-Type: application/json" \\
    -d '{
      "name": "My Corpus",
      "description": "Example corpus"
    }'`
      },
      {
        language: 'python', 
        code: `import requests

  response = requests.post(
      "https://api.vectara.io/v2/corpora",
      headers={
          "Authorization": "Bearer YOUR_API_KEY",
          "Content-Type": "application/json"
      },
      json={
          "name": "My Corpus",
          "description": "Example corpus"
      }
  )

  print(response.json())`
      },
      {
        language: 'javascript', 
        code: `const response = await fetch('https://api.vectara.io/v2/corpora', {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      name: 'My Corpus',
      description: 'Example corpus'
    })
  });

  const data = await response.json();
  console.log(data);`
      }
    ]}
  />

  // Disable auto-collapse
  <CodePanel
    collapsible={false}
    snippets={[{
      language: 'bash', 
      code: `# Install dependencies
  npm install

  # Run development server
  npm run dev`
    }]}
  />


 // Python API Example with Annotations
  <CodePanel
    layout="stacked"
    title="Query API Example"
    snippets={[{
      language: 'python',
      code: `import vectara
  from vectara.config import CustomerConfig, CorpusConfig

  # Initialize client with your credentials
  config = CustomerConfig(
      api_key="YOUR_API_KEY",
      customer_id="YOUR_CUSTOMER_ID"
  )

  client = vectara.Client(config)

  # Define your search query
  query = "What is machine learning?"
  corpus_id = 123456

  # Perform the search
  results = client.query(
      corpus_id=corpus_id,
      query=query,
      num_results=10,
      summarizer="vectara-summary-ext-v1.2.0"
  )

  # Process results
  for i, result in enumerate(results.documents):
      print(f"Result {i+1}:")
      print(f"  Score: {result.score}")
      print(f"  Text: {result.text[:100]}...")
      print(f"  Metadata: {result.metadata}")
      print()

  # Get the summary if available
  if results.summary:
      print(f"Summary: {results.summary}")
  `
    }]}
    annotations={{
      python: [
        { line: 6, text: "Replace with your actual API key" },
        { line: 7, text: "Find this in the Vectara console" },
        { line: 13, text: "Corpus ID from your dashboard" },
        { line: 20, text: "Choose from available summarizers" },
        { line: 26, text: "Relevance score between 0 and 1" }
      ]
    }}
    highlightLines="5-8,16-21"
  />

  // Multi-language Tabs with Annotations
  <CodePanel
    layout="stacked"
    title="Create Corpus Examples"
    tabs={true}
    snippets={[
      {
        language: 'curl',
        code: `curl -X POST https://api.vectara.io/v2/corpora \\
    -H "Authorization: Bearer YOUR_API_KEY" \\
    -H "Content-Type: application/json" \\
    -d '{
      "name": "Product Documentation",
      "description": "Technical documentation for our products",
      "filter_attributes": [
        {
          "name": "category",
          "type": "string",
          "indexed": true
        },
        {
          "name": "version",
          "type": "string",
          "indexed": true
        }
      ],
      "custom_dimensions": [
        {
          "name": "importance",
          "dimension": 0.8
        }
      ]
    }'`
      },
      {
        language: 'python',
        code: `from vectara import Client, CreateCorpusRequest, FilterAttribute

  # Create client instance
  client = Client(api_key="YOUR_API_KEY")

  # Define corpus configuration
  corpus_request = CreateCorpusRequest(
      name="Product Documentation",
      description="Technical documentation for our products",
      filter_attributes=[
          FilterAttribute(
              name="category",
              type="string",
              indexed=True
          ),
          FilterAttribute(
              name="version",
              type="string",
              indexed=True
          )
      ],
      custom_dimensions=[
          {
              "name": "importance",
              "dimension": 0.8
          }
      ]
  )

  # Create the corpus
  response = client.create_corpus(corpus_request)
  print(f"Created corpus with ID: {response.corpus_id}")
  `
      },
      {
        language: 'javascript',
        code: `import { VectaraClient } from '@vectara/client';

  // Initialize client
  const client = new VectaraClient({
    apiKey: 'YOUR_API_KEY'
  });

  // Define corpus configuration
  const corpusConfig = {
    name: 'Product Documentation',
    description: 'Technical documentation for our products',
    filterAttributes: [
      {
        name: 'category',
        type: 'string',
        indexed: true
      },
      {
        name: 'version',
        type: 'string',
        indexed: true
      }
    ],
    customDimensions: [
      {
        name: 'importance',
        dimension: 0.8
      }
    ]
  };

  // Create the corpus
  try {
    const response = await client.createCorpus(corpusConfig);
    console.log('Created corpus with ID:', response.corpusId);
  } catch (error) {
    console.error('Error creating corpus:', error);
  }
  `
      }
    ]}
    annotations={{
      curl: [
        { line: 2, text: "Get API key from console.vectara.com" },
        { line: 9, text: "Filter attributes for document metadata" },
        { line: 19, text: "Custom ranking dimensions" }
      ],
      python: [
        { line: 4, text: "Install with: pip install vectara" },
        { line: 11, text: "Filter attributes enable metadata filtering" },
        { line: 22, text: "Dimension value between 0 and 1" }
      ],
      javascript: [
        { line: 1, text: "Install with: npm install @vectara/client" },
        { line: 5, text: "API key from environment variable recommended" },
        { line: 26, text: "Higher dimension = higher importance" }
      ]
    }}
  />

  // Document Upload Example
  <CodePanel
    layout="stacked"
    title="Document Upload with Metadata"
    snippets={[{
      language: 'python',
      code: `import vectara
  from datetime import datetime
  import json

  client = vectara.Client(api_key="YOUR_API_KEY")

  # Document with structured sections
  document = {
      "document_id": "DOC-001",
      "title": "Introduction to Neural Networks",
      "metadata": {
          "author": "Dr. Sarah Johnson",
          "publication_date": "2024-03-15",
          "category": "machine-learning",
          "difficulty": "intermediate",
          "language": "en"
      },
      "sections": [
          {
              "text": "Neural networks are computational models inspired by biological neural networks.",
              "metadata": {
                  "section_type": "introduction",
                  "page_number": 1
              }
          },
          {
              "text": "A neural network consists of layers of interconnected nodes or neurons.",
              "metadata": {
                  "section_type": "overview",
                  "page_number": 2
              }
          },
          {
              "text": "Training involves adjusting weights through backpropagation.",
              "metadata": {
                  "section_type": "technical",
                  "page_number": 5
              }
          }
      ]
  }

  # Upload to corpus
  response = client.upload_document(
      corpus_id=123456,
      document=document
  )

  if response.success:
      print(f"Document uploaded successfully!")
      print(f"Document ID: {document['document_id']}")
  else:
      print(f"Upload failed: {response.error}")
  `
    }]}
    annotations={{
      python: [
        { line: 8, text: "Unique identifier for the document" },
        { line: 11, text: "Document metadata for filtering" },
        { line: 22, text: "Section-level metadata" },
        { line: 43, text: "Your corpus ID here" }
      ]
    }}
    highlightLines="10-17,41-45"
    initialCollapsedLines={20}
  />

  // Configuration Example
  <CodePanel
    layout="stacked"
    title="Advanced Query Configuration"
    snippets={[{
      language: 'javascript',
      code: `const vectara = require('@vectara/client');

  // Advanced query with all options
  const queryConfig = {
    corpus_id: 123456,
    query: "How do transformers work in NLP?",
    
    // Retrieval parameters
    num_results: 20,
    offset: 0,
    
    // Metadata filtering
    filter: {
      category: "nlp",
      difficulty: ["beginner", "intermediate"],
      publication_date: {
        gte: "2023-01-01"
      }
    },
    
    // Reranking configuration
    reranker: {
      type: "mmr",
      diversity_bias: 0.3
    },
    
    // Summary configuration
    summary: {
      enabled: true,
      summarizer: "vectara-summary-ext-v1.3.0",
      num_sentences: 5,
      response_lang: "en"
    },
    
    // Score threshold
    score_threshold: 0.5,
    
    // Include document metadata
    metadata_config: {
      include: ["author", "title", "publication_date"],
      exclude: ["internal_id"]
    }
  };

  // Execute query
  async function search() {
    try {
      const results = await vectara.query(queryConfig);
      
      console.log(\`Found \${results.documents.length} results\`);
      console.log(\`Summary: \${results.summary?.text}\`);
      
      // Process each result
      results.documents.forEach((doc, i) => {
        console.log(\`\nResult \${i + 1}:\`);
        console.log(\`  Score: \${doc.score}\`);
        console.log(\`  Title: \${doc.metadata.title}\`);
        console.log(\`  Text: \${doc.text.substring(0, 200)}...\`);
      });
    } catch (error) {
      console.error('Query failed:', error);
    }
  }

  search();
  `
    }]}
    annotations={{
      javascript: [
        { line: 5, text: "Get corpus ID from Vectara console" },
        { line: 13, text: "Complex metadata filter expressions" },
        { line: 23, text: "MMR = Maximum Marginal Relevance" },
        { line: 24, text: "0 = no diversity, 1 = max diversity" },
        { line: 30, text: "Latest summarizer model" },
        { line: 36, text: "Only return results above this score" },
        { line: 40, text: "Control which metadata fields to return" }
      ]
    }}
    highlightLines="12-19,27-33"
    collapsible={true}
    initialCollapsedLines={25}
  />

// Basic JavaScript editor
  <CodePanel
    layout="stacked"
    title="Try JavaScript"
    editable={true}
    snippets={[{
      language: 'javascript',
      code: `// Try editing and running this code!
  const greeting = "Hello, World!";
  console.log(greeting);

  // Calculate factorial
  function factorial(n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
  }

  console.log("5! =", factorial(5));

  // Array operations
  const numbers = [1, 2, 3, 4, 5];
  const doubled = numbers.map(n => n * 2);
  console.log("Doubled:", doubled);`
    }]}
  />

  // With custom runner for Python
  <CodePanel
    layout="stacked"
    title="Python Example"
    editable={true}
    onRun={async (code, language) => {
      // Send to your Python execution service
      const response = await fetch('/api/run-python', {
        method: 'POST',
        body: JSON.stringify({ code }),
        headers: { 'Content-Type': 'application/json' }
      });
      const result = await response.json();
      return result.output;
    }}
    snippets={[{
      language: 'python',
      code: `# This would require a server-side Python executor
  print("Hello from Python!")

  def greet(name):
      return f"Welcome, {name}!"

  print(greet("Vectara User"))`
    }]}
  />

  // Interactive API example
  <CodePanel
    layout="stacked"
    title="API Playground"
    editable={true}
    defaultOutput="Click Run to execute the API call"
    onRun={async (code) => {
      try {
        // Execute the code which should return a fetch promise
        const func = new Function('return ' + code);
        const response = await func();
        const data = await response.json();
        return JSON.stringify(data, null, 2);
      } catch (error) {
        return `Error: ${error.message}`;
      }
    }}
    snippets={[{
      language: 'javascript',
      code: `// Edit the endpoint or headers and click Run
  fetch('https://api.example.com/data', {
    method: 'GET',
    headers: {
      'Authorization': 'Bearer YOUR_TOKEN',
      'Content-Type': 'application/json'
    }
  })`
    }]}
  />

  <CodePanel
    layout="stacked"
    title="Try Vectara Search API"
    editable={true}
    snippets={[{
      language: 'javascript',
      code: `// Your credentials will be automatically injected
  const response = await fetch('https://api.vectara.io/v2/query', {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      corpus_id: YOUR_CORPUS_ID,
      query: "What is Vectara?",
      num_results: 5
    })
  });

  const data = await response.json();
  console.log('Search results:', data);`
    }]}
  />

   Now let me create updated examples that use the correct API format:

  // Example 1: Basic Query API Call
  <CodePanel
    layout="stacked"
    title="Query a Corpus"
    editable={true}
    snippets={[{
      language: 'javascript',
      code: `// Query a single corpus using the v2 API
  const response = await fetch('https://api.vectara.io/v2/corpora/YOUR_CORPUS_KEY/query', {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      query: "What is RAG?",
      limit: 10,
      offset: 0
    })
  });

  const results = await response.json();
  console.log('Search results:', results);

  // Display the results
  results.search_results?.forEach((result, i) => {
    console.log(\`Result \${i + 1}: \${result.text.substring(0, 100)}...\`);
    console.log(\`Score: \${result.score}\`);
  });`
    }]}
  />

  // Example 2: Multi-Corpus Query
  <CodePanel
    layout="stacked"
    title="Query Multiple Corpora"
    editable={true}
    snippets={[{
      language: 'javascript',
      code: `// Query multiple corpora at once
  const response = await fetch('https://api.vectara.io/v2/query', {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      query: "machine learning applications",
      search: {
        corpora: [
          {
            corpus_key: "YOUR_CORPUS_KEY",
            metadata_filter: "",
            lexical_interpolation: 0.025,
            custom_dimensions: {}
          },
          {
            corpus_key: "another-corpus-key",
            metadata_filter: "category = 'ML'",
            lexical_interpolation: 0.025
          }
        ],
        offset: 0,
        limit: 20
      }
    })
  });

  const data = await response.json();
  console.log('Multi-corpus results:', data);`
    }]}
  />

  // Example 3: Create a Corpus
  <CodePanel
    layout="stacked"
    title="Create a New Corpus"
    editable={true}
    tabs={true}
    snippets={[
      {
        language: 'curl',
        code: `curl -X POST https://api.vectara.io/v2/corpora \\
    -H "Authorization: Bearer YOUR_API_KEY" \\
    -H "Content-Type: application/json" \\
    -d '{
      "key": "my-new-corpus",
      "name": "My Documentation",
      "description": "Product documentation corpus",
      "queries_are_answers": false,
      "documents_are_questions": false,
      "encoder_name": "boomerang",
      "filter_attributes": [
        {
          "name": "category",
          "type": "string"
        }
      ]
    }'`
      },
      {
        language: 'javascript',
        code: `// Create a new corpus
  const response = await fetch('https://api.vectara.io/v2/corpora', {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      key: "my-new-corpus",
      name: "My Documentation",
      description: "Product documentation corpus",
      queries_are_answers: false,
      documents_are_questions: false,
      encoder_name: "boomerang",
      filter_attributes: [
        {
          name: "category",
          type: "string"
        }
      ]
    })
  });

  const corpus = await response.json();
  console.log('Created corpus:', corpus);
  console.log('Corpus key:', corpus.key);`
      },
      {
        language: 'python',
        code: `import requests
  import json

  # Create a new corpus
  response = requests.post(
      "https://api.vectara.io/v2/corpora",
      headers={
          "Authorization": "Bearer YOUR_API_KEY",
          "Content-Type": "application/json"
      },
      json={
          "key": "my-new-corpus",
          "name": "My Documentation",
          "description": "Product documentation corpus",
          "queries_are_answers": False,
          "documents_are_questions": False,
          "encoder_name": "boomerang",
          "filter_attributes": [
              {
                  "name": "category",
                  "type": "string"
              }
          ]
      }
  )

  corpus = response.json()
  print(f"Created corpus: {corpus}")
  print(f"Corpus key: {corpus.get('key')}")`
      }
    ]}
  />

  // Example 4: Upload Documents
  <CodePanel
    layout="stacked"
    title="Upload Documents to Corpus"
    editable={true}
    snippets={[{
      language: 'javascript',
      code: `// Upload a document with metadata
  const document = {
    id: "doc-001",
    type: "structured",
    title: "Introduction to RAG",
    metadata: {
      author: "Engineering Team",
      category: "tutorial",
      date_published: "2024-03-15"
    },
    sections: [
      {
        title: "What is RAG?",
        text: "Retrieval Augmented Generation (RAG) combines search with LLMs...",
        metadata: {
          section_type: "introduction"
        }
      },
      {
        title: "How RAG Works",
        text: "RAG systems first retrieve relevant documents, then generate responses...",
        metadata: {
          section_type: "technical"
        }
      }
    ]
  };

  const response = await fetch(\`https://api.vectara.io/v2/corpora/YOUR_CORPUS_KEY/documents\`, {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(document)
  });

  const result = await response.json();
  console.log('Document uploaded:', result);`
    }]}
  />

 <CodePanel
    layout="stacked"
    title="Try the Reset Button"
    editable={true}
    snippets={[{
      language: 'javascript',
      code: `// Original code - try editing this!
  console.log('Hello World!');
  const x = 5;
  console.log('x =', x);`
    }]}
  />


 <CodePanel
    editable={true}
    layout="stacked"
    title="Debug Reset Button"
    snippets={[{
      language: 'javascript',
      code: `// Try editing this
  console.log('Original code');`
    }]}
  />


# CodePanel Examples


This page demonstrates all CodePanel features with 10 examples, from simplest to most complex.

## Example 1: Basic Code Display

The simplest usage - displaying a single code snippet with default settings.

<CodePanel
  snippets={[
    {
      language: 'javascript',
      code: `// Hello World example
console.log("Hello, World!");
console.log("Welcome to Vectara!");`
    }
  ]}
/>

---

## Example 2: Multiple Language Support

Display code snippets in different languages with a language selector dropdown.

<CodePanel
  title="API Request Examples"
  snippets={[
    {
      language: 'curl',
      code: `curl -X POST https://api.vectara.io/v1/query \\
  -H "Authorization: Bearer YOUR_API_KEY" \\
  -H "Content-Type: application/json" \\
  -d '{"query": "What is Vectara?"}'`
    },
    {
      language: 'python',
      code: `import requests

response = requests.post(
    "https://api.vectara.io/v1/query",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={"query": "What is Vectara?"}
)`
    },
    {
      language: 'javascript',
      code: `const response = await fetch('https://api.vectara.io/v1/query', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ query: 'What is Vectara?' })
});`
    }
  ]}
/>

---

## Example 3: Stacked Layout with Custom Width

Full-width code panel using stacked layout with a custom maximum width.


<CodePanel
  title="Configuration File"
  layout="stacked"
  customWidth="800px"
  snippets={[
    {
      language: 'json',
      code: `{
  "vectara": {
    "customer_id": "123456789",
    "api_key": "YOUR_API_KEY",
    "indexing_endpoint": "https://api.vectara.io/v1/index",
    "serving_endpoint": "https://api.vectara.io/v1/query",
    "corpus": {
      "id": 1,
      "name": "my-corpus",
      "description": "Main document corpus",
      "filters": {
        "attributes": ["category", "author", "date"]
      }
    }
  }
}`
    }
  ]}
/>

---

## Example 4: Line Highlighting

Highlight specific lines or ranges to draw attention to important code sections.


<CodePanel
  title="Important Lines Example"
  highlightLines="3,5-7,11"
  snippets={[
    {
      language: 'python',
      code: `# Vectara SDK Example
import vectara

# Initialize the client (highlighted)
client = vectara.Client(api_key="YOUR_API_KEY")

# Create a corpus (highlighted)
corpus = client.create_corpus(
    name="product-docs"
)

# Index a document (highlighted)
response = corpus.index_document(
    document_id="doc-001",
    title="Getting Started",
    text="Welcome to our documentation..."
)`
    }
  ]}
/>

---

## Example 5: Tabs for Multiple Snippets

Use tabs instead of dropdown for better visual organization of related code snippets.


<CodePanel
  title="SDK Installation"
  tabs={true}
  snippets={[
    {
      language: 'bash',
      title: 'npm',
      code: `# Install Vectara SDK via npm
npm install @vectara/sdk

# Or using yarn
yarn add @vectara/sdk`
    },
    {
      language: 'bash',
      title: 'pip',
      code: `# Install Vectara SDK via pip
pip install vectara

# Or using poetry
poetry add vectara`
    },
    {
      language: 'bash',
      title: 'maven',
      code: `<!-- Add to your pom.xml -->
<dependency>
  <groupId>com.vectara</groupId>
  <artifactId>vectara-sdk</artifactId>
  <version>1.0.0</version>
</dependency>`
    }
  ]}
/>

---

## Example 6: Collapsible Long Code

Automatically collapse long code snippets to save space, with option to expand.


<CodePanel
  title="Complete API Response"
  collapsible={true}
  initialCollapsedLines={10}
  snippets={[
    {
      language: 'json',
      code: `{
  "status": "OK",
  "query_id": "qry_123456",
  "results": [
    {
      "document_id": "doc_001",
      "score": 0.95,
      "title": "Introduction to Vectara",
      "snippet": "Vectara is a powerful vector search platform...",
      "metadata": {
        "author": "John Doe",
        "date": "2024-01-15",
        "category": "tutorial",
        "tags": ["introduction", "getting-started", "basics"]
      },
      "sections": [
        {
          "section_id": "sec_001",
          "text": "Getting started with Vectara is easy...",
          "offset": 0,
          "length": 250
        },
        {
          "section_id": "sec_002",
          "text": "The platform provides powerful APIs...",
          "offset": 251,
          "length": 300
        }
      ]
    },
    {
      "document_id": "doc_002",
      "score": 0.89,
      "title": "Advanced Search Features",
      "snippet": "Explore advanced search capabilities...",
      "metadata": {
        "author": "Jane Smith",
        "date": "2024-02-01",
        "category": "advanced"
      }
    }
  ],
  "summary": {
    "total_results": 42,
    "processing_time_ms": 127,
    "query_understanding": {
      "intent": "informational",
      "entities": ["Vectara", "search", "platform"]
    }
  }
}`
    }
  ]}
/>

---

## Example 7: Line Annotations

Add helpful annotations to explain specific lines of code with hover tooltips.


<CodePanel
  title="Authentication Example"
  annotations={{
    python: [
      { line: 2, text: "Import the Vectara client library" },
      { line: 5, text: "Replace with your actual API key" },
      { line: 8, text: "Customer ID from your Vectara console" },
      { line: 12, text: "This creates a new search session" }
    ]
  }}
  snippets={[
    {
      language: 'python',
      code: `# Authentication setup
from vectara import Client, AuthConfig

# Configure authentication
auth_config = AuthConfig(
    api_key="vtr_1234567890abcdef",
    customer_id="123456789"
)

# Initialize client with auth
client = Client(auth_config)
session = client.create_session()`
    }
  ]}
/>

---

## Example 8: Live Editable Code

Enable live editing mode for interactive code examples that users can modify.


<CodePanel
  title="Try It Yourself"
  editable={true}
  defaultOutput="Click Reset to run the original code, or edit and see the changes!"
  snippets={[
    {
      language: 'javascript',
      code: `// Edit this code and see it in action!
const greeting = "Hello from Vectara";
const features = ["Vector Search", "NLP", "Real-time Indexing"];

console.log(greeting);
console.log("\\nKey Features:");
features.forEach((feature, i) => {
  console.log(\`\${i + 1}. \${feature}\`);
});

// Try adding your own code here!`
    }
  ]}
/>

---

## Example 9: Complex Multi-Feature Example

Combining multiple features: tabs, highlighting, annotations, and stacked layout.


<CodePanel
  title="Complete Integration Guide"
  layout="stacked"
  tabs={true}
  highlightLines="5-7,15-17"
  annotations={{
    javascript: [
      { line: 5, text: "Initialize with your credentials" },
      { line: 15, text: "Handle the search results" }
    ],
    python: [
      { line: 5, text: "Create client instance" },
      { line: 15, text: "Process search results" }
    ]
  }}
  snippets={[
    {
      language: 'javascript',
      title: 'Node.js',
      code: `// Vectara Search Integration
const { VectaraClient } = require('@vectara/sdk');

// Setup client
const client = new VectaraClient({
  apiKey: process.env.VECTARA_API_KEY,
  customerId: process.env.VECTARA_CUSTOMER_ID
});

// Perform search
async function search(query) {
  try {
    const results = await client.search({
      query: query,
      numResults: 10,
      corpusId: 1
    });
    
    return results.documents;
  } catch (error) {
    console.error('Search failed:', error);
    throw error;
  }
}`
    },
    {
      language: 'python',
      title: 'Python',
      code: `# Vectara Search Integration
from vectara import Client, SearchRequest
import os

# Setup client
client = Client(
    api_key=os.environ['VECTARA_API_KEY'],
    customer_id=os.environ['VECTARA_CUSTOMER_ID']
)

# Perform search
def search(query: str):
    try:
        request = SearchRequest(
            query=query,
            num_results=10,
            corpus_id=1
        )
        
        results = client.search(request)
        return results.documents
    except Exception as error:
        print(f"Search failed: {error}")
        raise`
    }
  ]}
/>

---

## Example 10: Advanced Editable Example with Custom Runner

The most complex example: editable code with custom execution handler for server-side processing.


<CodePanel
  title="Interactive Query Builder"
  editable={true}
  tabs={true}
  layout="stacked"
  defaultOutput="Modify the query and click Reset to execute"
  onRun={async (code, language) => {
    // Custom execution logic here
    if (language === 'json') {
      try {
        const query = JSON.parse(code);
        return `Query validated successfully!\n${JSON.stringify(query, null, 2)}`;
      } catch (e) {
        return `JSON Error: ${e.message}`;
      }
    }
    return `Language ${language} execution not implemented`;
  }}
  snippets={[
    {
      language: 'json',
      title: 'Query JSON',
      code: `{
  "query": "What is Vectara?",
  "search_params": {
    "corpus_ids": [1, 2, 3],
    "num_results": 10,
    "rerank": true,
    "reranker": {
      "type": "mmr",
      "diversity_factor": 0.3
    }
  },
  "metadata_filter": {
    "category": {
      "operator": "in",
      "values": ["tutorial", "guide", "reference"]
    },
    "date": {
      "operator": "gte",
      "value": "2024-01-01"
    }
  },
  "summary": {
    "enabled": true,
    "max_length": 200,
    "response_lang": "en"
  }
}`
    },
    {
      language: 'javascript',
      title: 'Query Builder',
      code: `// Build a Vectara query programmatically
const query = {
  query: "What is Vectara?",
  searchParams: {
    corpusIds: [1, 2, 3],
    numResults: 10,
    rerank: true,
    reranker: {
      type: "mmr",
      diversityFactor: 0.3
    }
  },
  metadataFilter: {
    category: {
      operator: "in",
      values: ["tutorial", "guide", "reference"]
    },
    date: {
      operator: "gte",
      value: "2024-01-01"
    }
  },
  summary: {
    enabled: true,
    maxLength: 200,
    responseLang: "en"
  }
};

// Convert to JSON
console.log("Generated Query:");
console.log(JSON.stringify(query, null, 2));`
    }
  ]}
/>


## Summary

These examples demonstrate the full range of CodePanel capabilities:

1. **Basic** - Simple code display
2. **Multi-language** - Language selector dropdown
3. **Layout** - Stacked layout with custom width
4. **Highlighting** - Line highlighting for emphasis
5. **Tabs** - Tab-based navigation
6. **Collapsible** - Auto-collapse long code
7. **Annotations** - Line-by-line explanations
8. **Editable** - Interactive code editing
9. **Combined** - Multiple features together
10. **Advanced** - Custom execution handlers

Each feature can be combined as needed to create the perfect code display for your documentation needs!